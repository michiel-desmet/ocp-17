/**
 * 1) must be declared in the root of project.
 * 2) module declaration must start with the module keyword
 * 3) naming is similar to the package naming
 */
module zoo.animal.feeding {
    exports zoo.animal.feeding; //Deze kan gebruikt worden.
}
